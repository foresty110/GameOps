# README Checklist

> README 작성/갱신 시 읽는다. 포트폴리오의 본체.

## 필수 항목
1. **프로젝트 한 줄 소개 + 시연 GIF** (맨 위)
2. **기술 스택** — 회사 스택과 일치한다는 점 명시
3. **아키텍처 다이어그램** (`docs/architecture.png`)
4. **ERD** (`docs/ERD.png`)
5. **주요 기능 스크린샷** (대시보드, 유저 관리, 메일, 상품)
6. **설계 의사결정 ("왜 이렇게 만들었는가")**
    - 왜 도메인 패키지 구조인가
    - 왜 Audit Log를 AOP로 처리했는가
    - 왜 Redis를 상품 목록에 캐싱했는가
    - 멱등성을 어떻게 보장하는가
    - 동시성이 중요한 지점은 어디인가
7. **실행 방법** — `docker-compose up` 한 줄로 끝나야 함
8. **테스트 실행 방법 + 커버리지 캡처**
9. **API 문서 링크** (Swagger UI 경로)
10. **앞으로의 개선 방향** — Kafka 도입 시 어디에, 모니터링(Sentry), K8s 배포

## 톤
- 면접관이 30초만 봐도 핵심이 보이게.
- 코드 잘 짠 것보다 **"왜 이렇게 짰는가"**가 보여야 함.